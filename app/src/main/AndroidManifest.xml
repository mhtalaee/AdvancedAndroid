<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="ir.goldenmind.advancedandroid">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".javaversion.BaseApplication"
            android:allowBackup="false"
            android:hardwareAccelerated="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".javaversion.activities.TestUiActivity">

        </activity>

        <activity android:name=".kotlinversion.activities.RetrofitActivity">

        </activity>
        <activity android:name=".javaversion.activities.RetrofitActivity">
        </activity>
        <activity android:name=".kotlinversion.activities.ContactListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".kotlinversion.activities.MainActivity">
        </activity>
    </application>

</manifest>